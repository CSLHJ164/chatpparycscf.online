  <div className="min-h-screen bg-gradient-to-br from-gray-50 to-blue-50">
      {/* Header */}
      <header className="bg-white shadow-sm sticky top-0 z-50 border-b border-gray-200">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-4">
          <div className="flex items-center justify-between">
            <div className="flex items-center space-x-3">
              <div className="w-12 h-12 bg-gradient-to-br from-green-400 to-emerald-600 rounded-2xl flex items-center justify-center shadow-lg">
                <Sprout className="w-7 h-7 text-white" />
              </div>
              <div>
                <h1 className="text-xl sm:text-2xl font-bold text-gray-900">Grow a Garden Calculator</h1>
                <p className="text-xs text-gray-500 hidden sm:block">Free Roblox Crop Value Tool</p>
              </div>
            </div>
            <div className="flex items-center space-x-2">
              <button
                onClick={() => setActiveTab('calculator')}
                className={`px-4 py-2 rounded-lg font-medium text-sm transition-all ${
                  activeTab === 'calculator'
                    ? 'bg-blue-500 text-white shadow-lg'
                    : 'bg-gray-100 text-gray-600 hover:bg-gray-200'
                }`}
              >
                Calculator
              </button>
              <button
                onClick={() => setActiveTab('wiki')}
                className={`px-4 py-2 rounded-lg font-medium text-sm transition-all ${
                  activeTab === 'wiki'
                    ? 'bg-blue-500 text-white shadow-lg'
                    : 'bg-gray-100 text-gray-600 hover:bg-gray-200'
                }`}
              >
                Wiki
              </button>
            </div>
          </div>
        </div>
      </header>

      <main className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        {activeTab === 'calculator' ? (
          <>
            {/* Hero */}
            <section className="text-center mb-8">
              <h2 className="text-3xl sm:text-4xl font-bold text-gray-900 mb-3">
                Calculate Your Crop Values
              </h2>
              <p className="text-lg text-gray-600 max-w-2xl mx-auto">
                Estimate fruit values, mutations, and trading power for Grow a Garden on Roblox
              </p>
            </section>

            {/* Main Calculator */}
            <div className="grid grid-cols-1 lg:grid-cols-3 gap-6 mb-8">
              {/* Calculator Panel */}
              <div className="lg:col-span-2 bg-white rounded-2xl shadow-xl p-6">
                <div className="flex items-center space-x-2 mb-6">
                  <Calculator className="w-6 h-6 text-blue-600" />
                  <h3 className="text-xl font-bold text-gray-900">Crop Value Calculator</h3>
                </div>

                {/* Crop Selection */}
                <div className="mb-6">
                  <label className="block text-sm font-semibold text-gray-700 mb-2">
                    Select Crop
                  </label>
                  <div className="relative">
                    <button
                      onClick={() => setShowCropDropdown(!showCropDropdown)}
                      className="w-full px-4 py-3 bg-gray-50 border-2 border-gray-200 rounded-xl text-left flex items-center justify-between hover:border-blue-400 transition-all"
                    >
                      {currentCrop ? (
                        <span className="flex items-center space-x-2">
                          <span className="text-2xl">{currentCrop.icon}</span>
                          <span className="font-medium">{currentCrop.name}</span>
                          <span className="text-sm text-gray-500">({currentCrop.baseValue} Sheckles)</span>
                        </span>
                      ) : (
                        <span className="text-gray-400">Choose a crop...</span>
                      )}
                      <ChevronDown className="w-5 h-5 text-gray-400" />
                    </button>
                    
                    {showCropDropdown && (
                      <div className="absolute w-full mt-2 bg-white rounded-xl shadow-2xl border border-gray-200 z-20 max-h-80 overflow-hidden">
                        <div className="p-3 border-b border-gray-100">
                          <div className="relative">
                            <Search className="absolute left-3 top-1/2 transform -translate-y-1/2 w-4 h-4 text-gray-400" />
                            <input
                              type="text"
                              placeholder="Search crops..."
                              value={searchTerm}
                              onChange={(e) => setSearchTerm(e.target.value)}
                              className="w-full pl-10 pr-4 py-2 border border-gray-200 rounded-lg text-sm focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                            />
                          </div>
                        </div>
                        <div className="overflow-y-auto max-h-64">
                          {filteredCrops.map((crop) => (
                            <button
                              key={crop.name}
                              onClick={() => {
                                setCurrentCrop(crop);
                                setShowCropDropdown(false);
                                setSearchTerm('');
                              }}
                              className="w-full px-4 py-3 hover:bg-blue-50 flex items-center justify-between transition-colors"
                            >
                              <div className="flex items-center space-x-3">
                                <span className="text-2xl">{crop.icon}</span>
                                <div className="text-left">
                                  <div className="font-medium text-gray-900">{crop.name}</div>
                                  <div className="text-xs text-gray-500">{crop.category}</div>
                                </div>
                              </div>
                              <span className="text-sm font-semibold text-blue-600">{crop.baseValue}</span>
                            </button>
                          ))}
                        </div>
                      </div>
                    )}
                  </div>
                </div>

                {/* Inputs Row */}
                <div className="grid grid-cols-3 gap-4 mb-6">
                  <div>
                    <label className="block text-sm font-semibold text-gray-700 mb-2">Weight (kg)</label>
                    <input
                      type="number"
                      value={weight}
                      onChange={(e) => setWeight(e.target.value)}
                      placeholder="1"
                      min="0.1"
                      step="0.1"
                      className="w-full px-4 py-2 border-2 border-gray-200 rounded-xl focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                    />
                  </div>
                  <div>
                    <label className="block text-sm font-semibold text-gray-700 mb-2">Quantity</label>
                    <input
                      type="number"
                      value={quantity}
                      onChange={(e) => setQuantity(e.target.value)}
                      placeholder="1"
                      min="1"
                      className="w-full px-4 py-2 border-2 border-gray-200 rounded-xl focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                    />
                  </div>
                  <div>
                    <label className="block text-sm font-semibold text-gray-700 mb-2">Friend Boost %</label>
                    <input
                      type="number"
                      value={friendBoost}
                      onChange={(e) => setFriendBoost(e.target.value)}
                      placeholder="0"
                      min="0"
                      max="100"
                      className="w-full px-4 py-2 border-2 border-gray-200 rounded-xl focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                    />
                  </div>
                </div>

                {/* Mutations */}
                <div className="space-y-4 mb-6">
                  <div>
                    <div className="flex items-center justify-between mb-2">
                      <label className="text-sm font-semibold text-gray-700">⭐ Growth Mutations</label>
                    </div>
                    <div className="grid grid-cols-3 gap-2">
                      {mutations.growth.map((mutation) => (
                        <button
                          key={mutation.name}
                          onClick={() => toggleMutation(mutation)}
                          className={`p-3 rounded-lg transition-all ${
                            selectedMutations.some(m => m.name === mutation.name)
                              ? 'bg-gradient-to-r from-blue-500 to-blue-600 text-white shadow-lg scale-105'
                              : 'bg-gray-50 hover:bg-gray-100 border border-gray-200'
                          }`}
                        >
                          <div className="text-xl mb-1">{mutation.icon}</div>
                          <div className="text-xs font-medium">{mutation.name}</div>
                          <div className="text-xs opacity-75">{mutation.multiplier}x</div>
                        </button>
                      ))}
                    </div>
                  </div>

                  <div>
                    <label className="block text-sm font-semibold text-gray-700 mb-2">❄️ Temperature Mutations</label>
                    <div className="grid grid-cols-3 gap-2">
                      {mutations.temperature.map((mutation) => (
                        <button
                          key={mutation.name}
                          onClick={() => toggleMutation(mutation)}
                          className={`p-3 rounded-lg transition-all ${
                            selectedMutations.some(m => m.name === mutation.name)
                              ? 'bg-gradient-to-r from-cyan-500 to-blue-500 text-white shadow-lg scale-105'
                              : 'bg-gray-50 hover:bg-gray-100 border border-gray-200'
                          }`}
                        >
                          <div className="text-xl mb-1">{mutation.icon}</div>
                          <div className="text-xs font-medium">{mutation.name}</div>
                          <div className="text-xs opacity-75">{mutation.multiplier}x</div>
                        </button>
                      ))}
                    </div>
                  </div>

                  <div>
                    <div className="flex items-center justify-between mb-2">
                      <label className="text-sm font-semibold text-gray-700">✨ Environmental Mutations</label>
                      <button
                        onClick={() => setShowMutationInfo(!showMutationInfo)}
                        className="text-blue-600 hover:text-blue-700"
                      >
                        <Info className="w-4 h-4" />
                      </button>
                    </div>
                    {showMutationInfo && (
                      <div className="mb-3 p-3 bg-blue-50 rounded-lg text-xs text-gray-700">
                        <p className="font-medium">⚠️ Note: Golden and Rainbow are mutually exclusive</p>
                      </div>
                    )}
                    <div className="grid grid-cols-4 gap-2">
                      {mutations.environmental.map((mutation) => (
                        <button
                          key={mutation.name}
                          onClick={() => toggleMutation(mutation)}
                          className={`p-2 rounded-lg transition-all ${
                            selectedMutations.some(m => m.name === mutation.name)
                              ? 'bg-gradient-to-r from-purple-500 to-pink-500 text-white shadow-lg scale-105'
                              : 'bg-gray-50 hover:bg-gray-100 border border-gray-200'
                          }`}
                        >
                          <div className="text-lg mb-1">{mutation.icon}</div>
                          <div className="text-xs font-medium">{mutation.name}</div>
                          <div className="text-xs opacity-75">{mutation.multiplier}x</div>
                        </button>
                      ))}
                    </div>
                  </div>
                </div>

                {/* Current Value Display */}
                <div className="bg-gradient-to-r from-green-50 to-emerald-50 rounded-2xl p-6 border-2 border-green-200 mb-4">
                  <div className="flex items-center justify-between">
                    <div>
                      <div className="text-sm text-gray-600 mb-1">Current Crop Value</div>
                      <div className="text-4xl font-bold text-green-600">
                        {currentValue.toLocaleString()} 💰
                      </div>
                      {totalMultiplier > 1 && (
                        <div className="text-sm text-gray-600 mt-2">
                          Total Multiplier: <span className="font-bold text-purple-600">{totalMultiplier}x</span>
                        </div>
                      )}
                    </div>
                    <button
                      onClick={addCropToList}
                      disabled={!currentCrop}
                      className="px-6 py-3 bg-blue-500 text-white rounded-xl font-semibold hover:bg-blue-600 disabled:bg-gray-300 disabled:cursor-not-allowed transition-all flex items-center space-x-2 shadow-lg"
                    >
                      <Plus className="w-5 h-5" />
                      <span>Add to List</span>
                    </button>
                  </div>
                </div>
              </div>

              {/* Portfolio Panel */}
              <div className="bg-white rounded-2xl shadow-xl p-6">
                <div className="flex items-center space-x-2 mb-4">
                  <BarChart3 className="w-6 h-6 text-purple-600" />
                  <h3 className="text-xl font-bold text-gray-900">Your Portfolio</h3>
                </div>

                <div className="bg-gradient-to-br from-purple-50 to-pink-50 rounded-xl p-4 mb-4 border-2 border-purple-200">
                  <div className="text-sm text-gray-600 mb-1">Total Value</div>
                  <div className="text-3xl font-bold text-purple-600">
                    {totalPortfolioValue.toLocaleString()}
                  </div>
                  <div className="text-xs text-gray-500 mt-1">{selectedCrops.length} crops</div>
                </div>

                <div className="space-y-2 max-h-96 overflow-y-auto">
                  {selectedCrops.length === 0 ? (
                    <div className="text-center py-8 text-gray-400">
                      <Sprout className="w-12 h-12 mx-auto mb-2 opacity-50" />
                      <p className="text-sm">No crops added yet</p>
                    </div>
                  ) : (
                    selectedCrops.map((item) => (
                      <div key={item.id} className="bg-gray-50 rounded-lg p-3 border border-gray-200 hover:border-blue-300 transition-all">
                        <div className="flex items-start justify-between mb-2">
                          <div className="flex items-center space-x-2">
                            <span className="text-2xl">{item.crop.icon}</span>
                            <div>
                              <div className="font-semibold text-gray-900 text-sm">{item.crop.name}</div>
                              <div className="text-xs text-gray-500">
                                {item.weight}kg × {item.quantity}
                              </div>
                            </div>
                          </div>
                          <button
                            onClick={() => removeCrop(item.id)}
                            className="text-red-500 hover:text-red-700 p-1"
                          >
                            <Trash2 className="w-4 h-4" />
                          </button>
                        </div>
                        {item.mutations.length > 0 && (
                          <div className="flex flex-wrap gap-1 mb-2">
                            {item.mutations.map(m => (
                              <span key={m.name} className="text-xs bg-purple-100 text-purple-700 px-2 py-1 rounded">
                                {m.icon} {m.name}
                              </span>
                            ))}
                          </div>
                        )}
                        <div className="text-right">
                          <span className="text-lg font-bold text-green-600">{item.value.toLocaleString()}</span>
                          <span className="text-xs text-gray-500 ml-1">Sheckles</span>
                        </div>
                      </div>
                    ))
                  )}
                </div>
              </div>
            </div>

            {/* Info Sections */}
            <div className="grid grid-cols-1 md:grid-cols-2 gap-6 mb-8">
              <div className="bg-white rounded-2xl p-6 shadow-md">
                <h2 className="text-2xl font-bold text-gray-900 mb-4">What is Grow a Garden?</h2>
                <p className="text-gray-700 leading-relaxed mb-3">
                  Grow a Garden is a wildly popular farming simulation game on Roblox, where players plant seeds, grow crops, and harvest fruits to earn Sheckles, the in-game currency.
                </p>
                <p className="text-gray-700 leading-relaxed">
                  With over 5 million concurrent players at its peak, this addictive game lets you expand your garden, unlock rare mutations like Shocked or Celestial, and trade items with others.
                </p>
              </div>

              <div className="bg-white rounded-2xl p-6 shadow-md">
                <h2 className="text-2xl font-bold text-gray-900 mb-4">About This Calculator</h2>
                <p className="text-gray-700 leading-relaxed mb-3">
                  The Grow a Garden Calculator is your ultimate tool for mastering the game. Calculate crop values with mutations, weight, friend bonuses, and track your entire portfolio.
                </p>
                <p className="text-gray-700 leading-relaxed">
                  Whether you're trading a Shocked Mango or optimizing farm profits, our calculator ensures you make informed decisions every time.
                </p>
              </div>
            </div>

            {/* How to Use */}
            <section className="bg-white rounded-2xl p-8 shadow-md mb-8">
              <h2 className="text-3xl font-bold text-gray-900 mb-6">How to Use the Calculator</h2>
              <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
                <div className="text-center">
                  <div className="w-16 h-16 bg-blue-100 rounded-full flex items-center justify-center mx-auto mb-4">
                    <span className="text-2xl">1️⃣</span>
                  </div>
                  <h3 className="font-bold text-lg mb-2">Select Crop</h3>
                  <p className="text-gray-600 text-sm">Choose your crop from the dropdown menu or search by name</p>
                </div>
                <div className="text-center">
                  <div className="w-16 h-16 bg-green-100 rounded-full flex items-center justify-center mx-auto mb-4">
                    <span className="text-2xl">2️⃣</span>
                  </div>
                  <h3 className="font-bold text-lg mb-2">Add Mutations</h3>
                  <p className="text-gray-600 text-sm">Click mutations to apply them and see multipliers stack</p>
                </div>
                <div className="text-center">
                  <div className="w-16 h-16 bg-purple-100 rounded-full flex items-center justify-center mx-auto mb-4">
                    <span className="text-2xl">3️⃣</span>
                  </div>
                  <h3 className="font-bold text-lg mb-2">Get Value</h3>
                  <p className="text-gray-600 text-sm">See instant results and add to your portfolio</p>
                </div>
              </div>
            </section>

            {/* Features */}
            <section className="mb-8">
              <h2 className="text-3xl font-bold text-gray-900 mb-6 text-center">Calculator Features</h2>
              <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">
                <div className="bg-gradient-to-br from-blue-500 to-blue-600 rounded-xl p-6 text-white text-center">
                  <Zap className="w-10 h-10 mx-auto mb-3" />
                  <h3 className="font-bold text-lg mb-2">Instant Calculations</h3>
                  <p className="text-sm opacity-90">Real-time value updates</p>
                </div>
                <div className="bg-gradient-to-br from-green-500 to-green-600 rounded-xl p-6 text-white text-center">
                  <Calculator className="w-10 h-10 mx-auto mb-3" />
                  <h3 className="font-bold text-lg mb-2">All Mutations</h3>
                  <p className="text-sm opacity-90">Complete mutation database</p>
                </div>
                <div className="bg-gradient-to-br from-purple-500 to-purple-600 rounded-xl p-6 text-white text-center">
                  <BarChart3 className="w-10 h-10 mx-auto mb-3" />
                  <h3 className="font-bold text-lg mb-2">Portfolio Tracking</h
